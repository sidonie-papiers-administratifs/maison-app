<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Maison : Bibliothèque + Cuisine</title>
  <meta name="theme-color" content="#0b0c10" />
  <link rel="manifest" href="manifest.json">
  <style>
    :root{
      --bg:#0b0c10; --card:#12141a; --muted:#9aa3b2; --txt:#eef2ff;
      --accent:#7c3aed; --accent2:#22c55e; --danger:#ef4444; --warn:#f59e0b;
      --line:rgba(255,255,255,.10);
      --radius:16px;
      --shadow: 0 10px 30px rgba(0,0,0,.35);
      font-family: system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial;
    }
    body{ margin:0; background: radial-gradient(1000px 600px at 10% 0%, rgba(124,58,237,.25), transparent),
                         radial-gradient(900px 500px at 90% 10%, rgba(34,197,94,.18), transparent),
                         var(--bg);
          color:var(--txt); }
    header{ position:sticky; top:0; z-index:9; backdrop-filter: blur(12px);
            background: rgba(11,12,16,.75); border-bottom:1px solid var(--line); }
    .wrap{ max-width:1100px; margin:0 auto; padding:16px; }
    h1{ font-size:20px; margin:0; letter-spacing:.2px; }
    .sub{ color:var(--muted); font-size:13px; margin-top:6px; line-height:1.35; }
    .tabs{ display:flex; gap:8px; margin-top:14px; flex-wrap:wrap; }
    .tabbtn{
      border:1px solid var(--line); background: rgba(255,255,255,.04); color:var(--txt);
      padding:10px 12px; border-radius:999px; cursor:pointer; font-weight:600; font-size:13px;
    }
    .tabbtn[aria-selected="true"]{ background: rgba(124,58,237,.22); border-color: rgba(124,58,237,.55); }
    main{ padding:18px 0 40px; }
    .grid{ display:grid; grid-template-columns: 1fr; gap:14px; }
    @media (min-width: 920px){ .grid{ grid-template-columns: 460px 1fr; align-items:start; } }
    .card{
      background: rgba(18,20,26,.92); border:1px solid var(--line); border-radius:var(--radius);
      box-shadow: var(--shadow); overflow:hidden;
    }
    .card h2{ margin:0; padding:14px 14px 0; font-size:16px; }
    .card .p{ padding:12px 14px 14px; color:var(--muted); font-size:13px; line-height:1.35; }
    .section{ padding:14px; border-top:1px solid var(--line); }
    label{ display:block; font-size:12px; color:var(--muted); margin-bottom:6px; }
    input, select, textarea{
      width:100%; box-sizing:border-box; padding:10px 10px; border-radius:12px;
      border:1px solid var(--line); background: rgba(255,255,255,.03); color:var(--txt);
      outline:none;
    }
    textarea{ min-height:84px; resize:vertical; }
    .row{ display:grid; grid-template-columns: 1fr; gap:10px; }
    @media (min-width: 520px){ .row.two{ grid-template-columns: 1fr 1fr; } }
    @media (min-width: 520px){ .row.three{ grid-template-columns: 1fr 1fr 1fr; } }
    .btnrow{ display:flex; gap:10px; flex-wrap:wrap; margin-top:10px; }
    button{
      border:1px solid var(--line); background: rgba(255,255,255,.04); color:var(--txt);
      padding:10px 12px; border-radius:12px; cursor:pointer; font-weight:700; font-size:13px;
    }
    button.primary{ background: rgba(124,58,237,.28); border-color: rgba(124,58,237,.55); }
    button.good{ background: rgba(34,197,94,.22); border-color: rgba(34,197,94,.55); }
    button.danger{ background: rgba(239,68,68,.18); border-color: rgba(239,68,68,.55); }
    button.warn{ background: rgba(245,158,11,.18); border-color: rgba(245,158,11,.55); }
    button:active{ transform: translateY(1px); }
    button[disabled]{ opacity:.5; cursor:not-allowed; }
    .list{ display:flex; flex-direction:column; }
    .item{
      padding:12px 14px; border-top:1px solid var(--line); display:flex; gap:12px; align-items:flex-start;
    }
    .badge{
      font-size:12px; padding:4px 10px; border-radius:999px; font-weight:800; white-space:nowrap;
      border:1px solid var(--line); background: rgba(255,255,255,.05);
    }
    .b-ok{ border-color: rgba(34,197,94,.55); background: rgba(34,197,94,.18); }
    .b-soon{ border-color: rgba(245,158,11,.60); background: rgba(245,158,11,.16); }
    .b-late{ border-color: rgba(239,68,68,.60); background: rgba(239,68,68,.16); }
    .meta{ color:var(--muted); font-size:12px; margin-top:3px; line-height:1.35; }
    .title{ font-weight:800; }
    .grow{ flex:1; min-width: 0; }
    .actions{ display:flex; gap:8px; flex-wrap:wrap; justify-content:flex-end; }
    .tiny{ padding:7px 9px; font-size:12px; border-radius:10px; font-weight:800; }
    .split{ display:flex; gap:10px; flex-wrap:wrap; justify-content:space-between; align-items:center; }
    .kpi{ display:flex; gap:8px; flex-wrap:wrap; }
    .pill{ border:1px solid var(--line); padding:6px 10px; border-radius:999px; color:var(--muted); font-size:12px; }
    .hidden{ display:none !important; }
    .note{ font-size:12px; color:var(--muted); line-height:1.35; }
    .hr{ height:1px; background:var(--line); margin:10px 0; }
    code{ font-size:12px; }
  </style>
</head>

<body>
<header>
  <div class="wrap">
    <h1>Maison : Bibliothèque + Cuisine</h1>
    <div class="sub">
      Hors-ligne + “ajout à l’écran d’accueil”.
      Rappels bibliothèque : <b>J-3</b>, <b>J-1</b>, <b>Aujourd’hui</b>.
    </div>
    <div class="tabs" role="tablist" aria-label="Onglets">
      <button class="tabbtn" role="tab" aria-selected="true" id="tab-biblio" aria-controls="panel-biblio">📚 Bibliothèque</button>
      <button class="tabbtn" role="tab" aria-selected="false" id="tab-cuisine" aria-controls="panel-cuisine">🍽️ Cuisine</button>
      <button class="tabbtn" role="tab" aria-selected="false" id="tab-sauvegarde" aria-controls="panel-sauvegarde">🗂️ Sauvegarde</button>
    </div>
  </div>
</header>

<main class="wrap">
  <!-- BIBLIO -->
  <section id="panel-biblio" role="tabpanel" aria-labelledby="tab-biblio">
    <div class="grid">
      <div class="card">
        <h2>Ajouter un livre</h2>
        <div class="p">Compteur de renouvellement + rappels (J-3, J-1, aujourd’hui).</div>

        <div class="section">
          <div class="row">
            <div>
              <label for="b_title">Titre du livre</label>
              <input id="b_title" placeholder="Ex : Le petit prince" />
            </div>
          </div>

          <div class="row two">
            <div>
              <label for="b_author">Auteur (optionnel)</label>
              <input id="b_author" placeholder="Ex : Antoine de Saint-Exupéry" />
            </div>
            <div>
              <label for="b_library">Bibliothèque (optionnel)</label>
              <input id="b_library" placeholder="Ex : Médiathèque du quartier" />
            </div>
          </div>

          <div class="row two">
            <div>
              <label for="b_borrow">Date d’emprunt</label>
              <input id="b_borrow" type="date" />
            </div>
            <div>
              <label for="b_due">Date de retour</label>
              <input id="b_due" type="date" />
            </div>
          </div>

          <div class="row two">
            <div>
              <label for="b_renew_count">Renouvellements déjà faits</label>
              <input id="b_renew_count" inputmode="numeric" placeholder="Ex : 0" />
            </div>
            <div>
              <label for="b_renew_max">Renouvellements max (optionnel)</label>
              <input id="b_renew_max" inputmode="numeric" placeholder="Ex : 1 ou 2" />
            </div>
          </div>

          <div class="row">
            <div>
              <label for="b_notes">Notes (optionnel)</label>
              <input id="b_notes" placeholder="Ex : Renouvelable / Tome 2 / etc." />
            </div>
          </div>

          <div class="btnrow">
            <button class="primary" id="b_add">Ajouter</button>
            <button id="b_reset">Vider le formulaire</button>
          </div>
        </div>

        <div class="section">
          <div class="split">
            <div>
              <div class="title">🔔 Rappels</div>
              <div class="meta">Toujours affichés ci-dessous. Notifications si autorisées.</div>
            </div>
            <div class="btnrow" style="margin:0;">
              <button class="good" id="notif_enable">Activer notifications</button>
              <button id="notif_test">Test</button>
            </div>
          </div>
          <div class="note" id="notif_note"></div>
          <div class="hr"></div>
          <div id="b_reminders" class="note"></div>
          <div class="note" style="margin-top:10px;">
            Si tu veux maximiser la fiabilité sur Android : autorise les notifications + désactive l’optimisation batterie pour ton navigateur.
          </div>
        </div>
      </div>

      <div class="card">
        <div class="section split" style="border-top:none;">
          <div>
            <h2 style="padding:0; margin:0;">Mes emprunts</h2>
            <div class="meta">Recherche, tri, filtres, renouvellements.</div>
          </div>
          <div class="kpi" id="b_kpi"></div>
        </div>

        <div class="section">
          <div class="row three">
            <div>
              <label for="b_search">Recherche</label>
              <input id="b_search" placeholder="Titre, auteur, bibliothèque…" />
            </div>
            <div>
              <label for="b_sort">Tri</label>
              <select id="b_sort">
                <option value="due_asc">Date de retour (proche → loin)</option>
                <option value="due_desc">Date de retour (loin → proche)</option>
                <option value="title_asc">Titre (A → Z)</option>
                <option value="title_desc">Titre (Z → A)</option>
                <option value="renew_desc">Renouvellements (haut → bas)</option>
              </select>
            </div>
            <div>
              <label for="b_filter">Filtre</label>
              <select id="b_filter">
                <option value="all">Tout</option>
                <option value="late">En retard</option>
                <option value="due0">Aujourd’hui</option>
                <option value="j1">J-1</option>
                <option value="j3">J-3</option>
                <option value="ok">OK</option>
              </select>
            </div>
          </div>
          <div class="btnrow">
            <button class="danger" id="b_clear_all">Tout supprimer</button>
          </div>
        </div>

        <div class="list" id="b_list"></div>
      </div>
    </div>
  </section>

  <!-- CUISINE -->
  <section id="panel-cuisine" class="hidden" role="tabpanel" aria-labelledby="tab-cuisine">
    <div class="grid">
      <div class="card">
        <h2>Ajouter un aliment</h2>
        <div class="p">Stock + dates limites + idées de recettes (mode halal Maghreb/Méditerranée).</div>
        <div class="section">
          <div class="row two">
            <div>
              <label for="p_name">Nom</label>
              <input id="p_name" placeholder="Ex : œufs / tomates / lentilles…" />
            </div>
            <div>
              <label for="p_cat">Catégorie</label>
              <select id="p_cat">
                <option value="Divers">Divers</option>
                <option value="Frais">Frais</option>
                <option value="Épicerie">Épicerie</option>
                <option value="Surgelé">Surgelé</option>
                <option value="Conserve">Conserve</option>
                <option value="Épices">Épices</option>
              </select>
            </div>
          </div>

          <div class="row three">
            <div>
              <label for="p_qty">Quantité (optionnel)</label>
              <input id="p_qty" placeholder="Ex : 6 / 500g / 1 brique…" />
            </div>
            <div>
              <label for="p_exp">Date limite (optionnel)</label>
              <input id="p_exp" type="date" />
            </div>
            <div>
              <label for="p_min">Seuil “bientôt périmé”</label>
              <select id="p_min">
                <option value="3">3 jours</option>
                <option value="5">5 jours</option>
                <option value="7">7 jours</option>
              </select>
            </div>
          </div>

          <div class="btnrow">
            <button class="primary" id="p_add">Ajouter</button>
            <button id="p_reset">Vider le formulaire</button>
          </div>
        </div>

        <div class="hr"></div>

        <h2 style="padding:0 14px;">Mode recettes</h2>
        <div class="p">Filtre halal Maghreb/Méditerranée + exclusion alcool.</div>
        <div class="section">
          <div class="row two">
            <div>
              <label for="mode_region">Filtre</label>
              <select id="mode_region">
                <option value="all">Tout</option>
                <option value="maghreb_med">Halal Maghreb / Méditerranée</option>
                <option value="user_only">Mes recettes uniquement</option>
              </select>
            </div>
            <div>
              <label for="mode_strict">Alcool</label>
              <select id="mode_strict">
                <option value="strict" selected>Strict : exclure toute recette avec alcool</option>
                <option value="normal">Normal : ne rien exclure</option>
              </select>
            </div>
          </div>
        </div>

        <div class="hr"></div>

        <h2 style="padding:0 14px;">Ajouter ta recette</h2>
        <div class="p">Ajoute tes recettes perso (et indique Maghreb/Méditerranée/Autre).</div>
        <div class="section">
          <div class="row">
            <div>
              <label for="r_title">Nom de la recette</label>
              <input id="r_title" placeholder="Ex : Chorba / tajine / poisson au four…" />
            </div>
          </div>
          <div class="row two">
            <div>
              <label for="r_region">Type</label>
              <select id="r_region">
                <option value="Maghreb">Maghreb</option>
                <option value="Mediterranee">Méditerranée</option>
                <option value="Autre">Autre</option>
              </select>
            </div>
            <div>
              <label for="r_alcohol">Contient de l’alcool ?</label>
              <select id="r_alcohol">
                <option value="no" selected>Non</option>
                <option value="yes">Oui</option>
              </select>
            </div>
          </div>
          <div class="row">
            <div>
              <label for="r_ings">Ingrédients (séparés par des virgules)</label>
              <input id="r_ings" placeholder="Ex : tomates, oignon, ail, coriandre, cumin" />
            </div>
          </div>
          <div class="row">
            <div>
              <label for="r_steps">Étapes (courtes)</label>
              <textarea id="r_steps" placeholder="1) ... 2) ... 3) ..."></textarea>
            </div>
          </div>
          <div class="btnrow">
            <button class="good" id="r_add">Ajouter la recette</button>
            <button class="danger" id="r_clear_all">Supprimer toutes mes recettes</button>
          </div>
        </div>
      </div>

      <div class="card">
        <div class="section split" style="border-top:none;">
          <div>
            <h2 style="padding:0;margin:0;">Mon stock + idées repas</h2>
            <div class="meta">Tri, filtres, et recettes qui matchent.</div>
          </div>
          <div class="kpi" id="p_kpi"></div>
        </div>

        <div class="section">
          <div class="row three">
            <div>
              <label for="p_search">Recherche</label>
              <input id="p_search" placeholder="Nom, catégorie…" />
            </div>
            <div>
              <label for="p_sort">Tri</label>
              <select id="p_sort">
                <option value="name_asc">Nom (A → Z)</option>
                <option value="name_desc">Nom (Z → A)</option>
                <option value="exp_asc">Date limite (proche → loin)</option>
                <option value="exp_desc">Date limite (loin → proche)</option>
                <option value="cat_asc">Catégorie</option>
              </select>
            </div>
            <div>
              <label for="p_filter">Filtre</label>
              <select id="p_filter">
                <option value="all">Tout</option>
                <option value="expiring">Bientôt périmé</option>
                <option value="expired">Périmé</option>
                <option value="noexp">Sans date</option>
              </select>
            </div>
          </div>

          <div class="btnrow">
            <button class="danger" id="p_clear_all">Tout supprimer</button>
          </div>
        </div>

        <div class="list" id="p_list"></div>

        <div class="section">
          <h2 style="padding:0;margin:0;">Idées de recettes</h2>
          <div class="p" style="padding:10px 0 0;">Score = ingrédients trouvés / nécessaires.</div>
          <div class="btnrow">
            <button class="primary" id="suggest">Générer les idées</button>
          </div>
        </div>
        <div class="list" id="s_list"></div>
      </div>
    </div>
  </section>

  <!-- SAUVEGARDE -->
  <section id="panel-sauvegarde" class="hidden" role="tabpanel" aria-labelledby="tab-sauvegarde">
    <div class="card">
      <h2>Export / Import</h2>
      <div class="p">Pour changer d’appareil : exporte ton JSON et réimporte-le.</div>
      <div class="section">
        <div class="btnrow">
          <button class="primary" id="export_btn">Exporter mes données</button>
          <button id="import_btn">Importer (coller du JSON)</button>
          <button class="danger" id="wipe_btn">Tout effacer</button>
        </div>
        <div class="hr"></div>
        <label for="io_area">Zone import/export</label>
        <textarea id="io_area" placeholder="Clique 'Exporter', ou colle ici ton JSON puis clique 'Importer'."></textarea>
        <div class="note">Données stockées localement (localStorage) + export manuel.</div>
      </div>
    </div>
  </section>
</main>

<script>
/* =========================
   PWA / SW
========================= */
const STORAGE_KEY = "maison_biblio_cuisine_v3";
const REMIND_DAYS = [3, 1, 0]; // ✅ correction: J-3, J-1, Aujourd’hui

async function registerSW(){
  if(!("serviceWorker" in navigator)) return;
  try{
    const reg = await navigator.serviceWorker.register("./service-worker.js");
    // sync state to SW after registration
    await syncStateToSW();
    return reg;
  }catch(e){
    // ignore
  }
}

function postToSW(msg){
  if(!navigator.serviceWorker?.controller) return;
  navigator.serviceWorker.controller.postMessage(msg);
}

async function syncStateToSW(){
  postToSW({ type:"STATE_SYNC", payload: loadState() });
}

/* =========================
   Helpers
========================= */
function loadState(){
  try{
    const raw = localStorage.getItem(STORAGE_KEY);
    if(!raw) return { books: [], pantry: [], recipes: [], notif:{ enabled:false } };
    const parsed = JSON.parse(raw);
    return {
      books: Array.isArray(parsed.books) ? parsed.books : [],
      pantry: Array.isArray(parsed.pantry) ? parsed.pantry : [],
      recipes: Array.isArray(parsed.recipes) ? parsed.recipes : [],
      notif: parsed.notif && typeof parsed.notif === "object" ? parsed.notif : { enabled:false }
    };
  }catch(e){
    return { books: [], pantry: [], recipes: [], notif:{ enabled:false } };
  }
}

function saveState(){
  localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
  renderAll();
  syncStateToSW();
}

function uid(){
  return Math.random().toString(16).slice(2) + Date.now().toString(16);
}

function todayISO(){
  const d = new Date();
  d.setHours(0,0,0,0);
  return d.toISOString().slice(0,10);
}

function parseISO(s){
  if(!s) return null;
  const d = new Date(s + "T00:00:00");
  return isNaN(d.getTime()) ? null : d;
}

function daysBetween(a,b){
  const ms = 24*60*60*1000;
  const A = new Date(a); A.setHours(0,0,0,0)
